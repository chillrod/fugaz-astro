---
import { BUILDER_URLS, fetchBuilderContent } from "../services/builder-io";
import MenuIcon from "./MenuIcon.astro";

const header = await fetchBuilderContent(BUILDER_URLS.home.header);
---

<footer
  class="mt-12 bg-grid bg-grid-size p-6 flex justify-between items-center"
>
  <h2 class="-tracking-widest text-6xl font-bold text-orange-500">
    {header[0].data.title}
  </h2>

  <a href="/">
    <MenuIcon name="ant-design:arrow-up" text="Início" />
  </a>
</footer>
